# Snake Game (Pygame)

Simple Snake game implemented in Python using Pygame.

Requirements
- Python 3.8+
- Install dependencies:

```powershell
python -m pip install -r requirements.txt
```

Run

```powershell
python main.py
```

Controls: Arrow keys or WASD. Press `R` to restart, `Esc` to quit.
